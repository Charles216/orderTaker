<li>
	{{name}}

	<button class="change-served" data-id="{{id}}" data-newOrder="{{burger_served}}">
		{{#if burger_served}}Burger Served!{{else}}Burger Ordered!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>

