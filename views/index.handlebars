<h1>Cats!</h1>

<h2>Cats that are not sleepy!</h2>

<ul>
  {{#each burgers}}
    {{#unless burger_served}}
      {{> burgers/burger-block burger_served=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been served!</h2>

<ul>
  {{#each burgers}}
    {{#if burger_served}}
      {{> burgers/burger-block burger_served=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">Cat Name:</label>
    <input type="text" id="burg" name="name">
  </div>

  <div class="form-group">
    <label for="served">Burger served yet?</label><br>
    <input type="radio" name="served" value="1" checked> Served!<br>
    <input type="radio" name="served" value="0"> Unserved
  </div>

  <button type="submit">Add Burger</button>
</form>
